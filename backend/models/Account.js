class Account {

    constructor(account_id, name, password, created_on, last_login, is_admin) {
        this.account_id = account_id;
        this.name = name;
        this.password = password;
        this.created_on = created_on;
        this.last_login = last_login;
        this.is_admin = is_admin;
    }

}